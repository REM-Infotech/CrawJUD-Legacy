version: '3.9'
services:
  app:
    build:
      context: .
    volumes:
      - .:/crawjud_backend
    command: poetry run python -m app

    ports:
      - 5000:5000
  celery:
    build:
        context: .
    volumes:
      - .:/crawjud_backend
    command: poetry run python -m celery -A app.__main__.celery -E worker -B --loglevel=info -P eventlet

